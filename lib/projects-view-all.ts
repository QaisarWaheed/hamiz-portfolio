/** Display cap for the homepage projects grid (not a hardcoded portfolio size). */
export { PROJECTS_DISPLAY_LIMIT } from "@/lib/data/projects";

/** TODO: set true when /work index route ships; link targets /work. */
export const WORK_INDEX_ENABLED = false;

export const WORK_INDEX_HREF = "/work";
